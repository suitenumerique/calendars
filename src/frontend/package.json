{
  "name": "main",
  "version": "0.10.1",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w calendars",
    "build": "npm run build -w calendars",
    "lint": "npm run lint -w calendars",
    "build-theme": "npm run build-theme -w calendars"
  },
  "devDependencies": {
    "jsdom": "27.4.0"
  }
}
